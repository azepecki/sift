def int main() {
	str regex = "rk";
	str valid_email = "rk3142@columbia.edu";
	str invalid_email = "rk3142";
        

	if (reg_test(valid_email, regex)) {
		str result_str = str_add(valid_email, " is valid");
		print(result_str);
	} else {
		print((str_add(valid_email, " is an invalid website url")));
	}


	return 0;

}